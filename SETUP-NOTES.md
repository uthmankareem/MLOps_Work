# Project Setup Notes

Notes from the assistant's first collaborative branch.

## Current state (as of resume)
- Repo: `MLOps_Work` (public), owner `uthmankareem`
- Local clone: `C:\Users\Dell\OneDrive\Desktop\MLOps_Work`
- Identity: `uthmankareem` / `kareemuthman305@gmail.com`
- Commits pushed to `origin/main`: `0e85265` (Add README), `ada82d8` (Add git cheat sheet)

## How we collaborate (single-account team workflow)
- `main` = stable, reviewed code.
- Feature branches = work in progress (`branch` + `commit`).
- Pull request = review + merge back to `main`.
- This mimics a real team using one shared account.

## Commands learned
See GIT-CHEATSHEET.md for the full reference.